# 无网格法知识专题

共 9 篇：6 篇由项目原文适配生成，3 篇为学习路径与验证说明。站内入口 `/meshfree`，知识集合 `/knowledge?collection=meshfree`。

运行 `npm run knowledge:validate -- templates/knowledge/meshfree` 校验，`npm run knowledge:import -- templates/knowledge/meshfree` 导入；全量 `knowledge:sync` 同样包含本目录。

原始参考保持只读。原文副本位于 `public/meshfree/documents/`，模型、媒体与校核证据位于 `public/meshfree/`。来源和集成步骤见 `docs/MESHFREE_INTEGRATION.md`。
